# Locked schema — CLI mode.
# Requires real `claude` binary in PATH and state.json with init_agent="claude_code".
# Used by SECONDSIGHT_TEST_REAL_CLI=1 gated tests.

[analysis.cli]
default_agent = "auto"

[general]
mode = "cli"
