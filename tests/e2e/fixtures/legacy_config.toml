# Synthetic pre-toggle config — flat [analysis] default_agent.
# No [general] section, no [analysis.cli], no [providers.*].
# Used to test the DC12 legacy-upgrade path.
# Expected loader behavior:
# - WARN log: "legacy [analysis] default_agent found..."
# - general.mode defaults to "cli" (built-in default)
# - precheck fails with state_missing when state.json is absent

[analysis]
default_agent = "claude_code"
