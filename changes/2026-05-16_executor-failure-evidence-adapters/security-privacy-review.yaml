feature: executor-failure-evidence-adapters
status: pass_after_fix

review_scope:
  base_ref: "5e9bb7c"
  head_ref: "HEAD"
  files_reviewed: 23
  review_type: "manual_security_privacy_review"
  dedicated_platform_tool_available: false
  note: "No separate built-in security/privacy review tool is exposed in this environment; reviewed the committed diff manually and recorded this visible degradation."

issues_found:
  - id: SPR-1
    severity: medium
    status: fixed
    files:
      - src/secondsight/analysis/cli_dispatcher.py
      - src/secondsight/analysis/sdk_dispatcher.py
    description: "Failure summaries could include raw CLI stderr or provider error text in session_summary.body. error_details was sanitized, but summaries are also persisted and could leak tokens/API keys."
    fix_commit: "3e4560c"
    verification:
      - "tests/analysis/test_cli_dispatcher.py::TestDC6_SubprocessNonZeroExit::test_nonzero_exit_redacts_secret_from_session_summary_body"
      - "tests/analysis/test_sdk_dispatcher.py::test_sdk_failure_output_redacts_secret_from_session_summary_body"

final_review:
  result: pass
  issues_remaining: 0
  accepted_security_risks: []
  privacy_notes:
    - "Adapter evidence may preserve stderr/stdout excerpts, but persisted error_details and summary messages now pass through the shared sanitizer and string bounds."
    - "No new network calls, credential storage, subprocess shell usage, or database schema changes were introduced."

validation:
  - command: "uv run pytest tests/analysis/test_output_recovery.py tests/analysis/test_cli_adapters.py tests/analysis/test_cli_dispatcher.py tests/analysis/test_sdk_dispatcher.py -q"
    result: "132 passed"
  - command: "uv run pre-commit run --files src/secondsight/analysis/cli_dispatcher.py src/secondsight/analysis/sdk_dispatcher.py tests/analysis/test_cli_dispatcher.py tests/analysis/test_sdk_dispatcher.py"
    result: "passed"
